{% extends 'base.html.twig' %}

{% block content %}
    {{ parent() }}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    <br>
    {{ regime.id }}
    {{ regime.title }}
    {{ regime.description }}
    {{ regime.rating }}
{% endblock %}
