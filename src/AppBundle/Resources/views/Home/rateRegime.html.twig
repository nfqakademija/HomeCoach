{% extends 'base.html.twig' %}

{% block body %}
    {{ parent() }}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    </br>
    {{ regime.id }}
    {{ regime.title }}
    {{ regime.describtion }}
    {{ regime.rating }}
{% endblock %}
