{% block content %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    {%  if app.user.activeregime is not null  %}
    Programa aktyvuota!
    {% endif %}
    <br>
{% endblock %}
