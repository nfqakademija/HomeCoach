{% block content %}
    {% if form is not null %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    {%  if app.user.activeworkout is not null  %}
    Programa aktyvuota!
    {% endif %}
    {% endif %}
    <br>
{% endblock %}
